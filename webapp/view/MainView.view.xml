<mvc:View controllerName="DXC.invoices.controller.MainView"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Page id="page" title="{i18n>title}">
        <content>
            <Panel headerText="{i18n>selectionScreen}">
                <Label id="labelShipName" 
                       labelFor="shipName" 
                       text="{i18n>shipName}" 
                       class="sapUiSmallMargin"></Label>
                <Input id="shipName" 
                       width="200px" 
                       class="sapUiSmallMarginEnd"></Input>
                <Label id="labelCountry" 
                       text="{i18n>country}" 
                       class="sapUiSmallMargin"></Label>
                <Select id="slCountry" 
                        selectedKey="{selectionScreen>/CountryKey}" 
                        forceSelection="false" 
                        items="{selectionScreen>/Countries}">
                    <items>
                        <core:Item key="{selectionScreen>key}" text="{selectionScreen>text}"></core:Item>
                    </items></Select>
            </Panel>
        </content>
    </Page>
</mvc:View>
